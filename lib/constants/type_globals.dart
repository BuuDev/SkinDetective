enum ETextField {
  textArea,
  none,
}

enum EButton {
  full,
  normal,
}

enum EDot {
  horizontal,
  vertical,
}

enum EShapeDot {
  rectangle,
  circle,
}

enum ELanguage { vn, en }

enum EAcneAnalysis {
  start,
  done,
  normal,
}
